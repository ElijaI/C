#include <stdio.h>
#include "vektoren.h"

int main(void) {
	struct vektor_s p1, p2, pe;

	printf("Geben Sie den ersten Vektor ein: \n");
	scanf("%f %f", &p1.x, &p1.y);

	printf("Geben Sie den zweiten Vektor ein: \n");
	scanf("%f %f", &p2.x, &p2.y);

}

strukt vektsum(p1, p2) {
	
}

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(int argc, char *argv []) {
	if (argc != 2) {
		printf("use: ./fehlerteufel <anzahl fehler>\n");
	}
	FILE *in, *out;
	char c;
	int random;
	int count = 0;

	in 

